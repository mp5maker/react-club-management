export enum BACKEND {
  URI = 'http://localhost:4000',
}

export enum PERSIST {
  THEME = 'theme',
  LANGUAGE = 'language'
}

export enum THEME {
  LIGHT = 'light',
  DARK = 'dark'
}

export enum LANGUAGE {
  ENGLISH = 'en',
  LITHUANIAN = 'lt'
}